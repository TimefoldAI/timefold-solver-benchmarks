# Not including this file with your benchmarks will execute all move providers.

# Possible values: change, mass_change, swap, pillar_change, sub_pillar_change, pillar_swap,
#                  sub_pillar_swap, assign, unassign, mass_assign, mass_unassign, pillar_unassign,
#                  sub_pillar_unassign
# Not including this will cause all basic-variable move providers to be executed.
basic_move_provider=change,mass_change,swap,pillar_change,sub_pillar_change,pillar_swap,sub_pillar_swap,assign,unassign,mass_assign,mass_unassign,pillar_unassign,sub_pillar_unassign

# Possible values: list_change, list_swap, list_tail_swap, two_opt_list, mass_list_change,
#                  sub_list_change, sub_list_swap, list_assign, list_unassign, mass_list_assign,
#                  mass_list_unassign, sub_list_unassign
# Not including this will cause all list-variable move providers to be executed.
list_move_provider=list_change,list_swap,list_tail_swap,two_opt_list,mass_list_change,sub_list_change,sub_list_swap,list_assign,list_unassign,mass_list_assign,mass_list_unassign,sub_list_unassign

# CI runs 40 forks, 4 warmup iterations and 1 measurement iteration;
# see the "Configure the benchmark" step of .github/workflows/performance_move_provider.yml for why.

# How many forks to run each move provider with.
# Default is 10.
#forks=10

# How many warmup iterations to run each move provider with.
# Default is 5.
#warmup_iterations=5

# How many measurement iterations to run each move provider with.
# Default is 5.
#measurement_iterations=5

# Ratio of benchmark score error to benchmark score above which warnings will be output.
# The higher the threshold, the less reliable the benchmark score is.
# Default is 0.02 (±2%).
#relative_score_error_threshold=0.02
